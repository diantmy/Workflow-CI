name: telco-churn-env

channels:
  - defaults
  - conda-forge

dependencies:
  - python=3.11
  - pip
  - pip:
      - pandas
      - numpy
      - scikit-learn
      - mlflow
      - matplotlib
      - seaborn
      - joblib
